@model IEnumerable<SAT.DATA.EF.ScheduledClass>

@{
    ViewBag.Title = "Scheduled Class";
}


@if (User.IsInRole("Admin") || User.IsInRole("Scheduling"))
{
<p>
    <a class="btn btn-primary btn-sm" href="@Url.Action("Create", "ScheduledClasses")">Create</a>
</p>
}
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.StartDate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.EndDate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.InstructorName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Location)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Course.CourseName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ScheduledClassStatus.SCSName)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.StartDate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.EndDate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.InstructorName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Location)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Course.CourseName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ScheduledClassStatus.SCSName)
        </td>
        <td>

            @if (User.IsInRole("Admin") || User.IsInRole("Scheduling"))
            {
                @*@Html.ActionLink("Edit", "Edit", new { id=item.ScheduledClassId }, new { @class = "btn btn-warning btn-sm" })*@
            }

                @Html.ActionLink("Details", "Details", new { id=item.ScheduledClassId }, new { @class = "btn btn-primary btn-sm" }) 

            @if (User.IsInRole("Admin"))
            {
                
                @Html.ActionLink("Delete", "Delete", new { id=item.ScheduledClassId }, new { @class = "btn btn-danger btn-sm" })
            }
        </td>
    </tr>
}

</table>
